{
  "name": "my-stackflow-app-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "web": "pnpm --filter web-app dev",
    "mobile": "pnpm --filter mobile start",
    "android": "pnpm --filter mobile android",
    "ios": "pnpm --filter mobile ios",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
